doctype html
html

  head
    title Users List
    link(rel="stylesheet" href="/style")
    block styles

  body

    div(class="container")
      article
        h1 Liste des utilisateurs
        ul
          each user in users
            li= `${user.name} naquis le ${user.birth}`
              a(href=`/delete/${user.id}` class="delbtn") X