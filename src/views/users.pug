doctype html
html
  head
    title Users List
    link(rel="stylesheet" href="/style")
    block styles

  body
    h1 List of Users
    ul
      each user in users
        li= `${user.name} est né(e) le ${user.birth}`